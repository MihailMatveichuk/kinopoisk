import CustomSelect from '../CustomSelect/CustomSelect';
import './leftBar.sass';

const LeftBar = () => {
  return (
    <section className='side__wrapper'>
      <CustomSelect />
    </section>
  );
};

export default LeftBar;
